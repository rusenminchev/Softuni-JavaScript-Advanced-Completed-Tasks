function getLargestNumber(firstNum, secondNum, thirdNum){
    let largestNumber = Math.max(firstNum, secondNum, thirdNum);
    
    console.log('The largest number is ' + largestNumber + '.');
    }
    
    getLargestNumber(5, -3, 16);